@* @model InmobiliariaCA.Models.Propietario
<div class="container mt-5">
    <div class="card">
        <div class="card-header text-center">
            <h3 class="card-title">Detalles del Propietario</h3>
        </div>
        <div class="card-body">
            <div class="card-text">
                <strong>Apellido:</strong> @Model.Apellido <br />
                <strong>Nombre:</strong> @Model.Nombre <br />
                <strong>Documento:</strong> @Model.Dni <br />
                <strong>Teléfono:</strong> @Model.Telefono <br />
                <strong>Email:</strong> @Model.Email <br />
                <strong id="direccion">Dirección:</strong> @Model.Direccion <br />
                <strong>Fecha Creacion:</strong> @Model.Fecha_Creacion <br />
                <strong>Fecha Actualizacion:</strong> @Model.Fecha_Actualizacion <br />
            </div>
        </div>
        <div class="card-footer text-end">
            <a href="@Url.Action("AltaEditar", "Propietario", new { id = Model.Id })" class="btn btn-warning">Editar</a>
            <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#modalBajaLogica">
                Eliminar
            </button>
            <a href="@Url.Action("Index", "Propietario")" class="btn btn-primary">Volver a la lista</a>

        </div>
    </div>
</div>

<!-- Button trigger modal -->


<!-- Modal -->
<div class="modal fade" id="modalBajaLogica" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="modal-title fs-5" id="exampleModalLabel">Propiestario</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                ¿Esta seguro que desea eliminar a @Model.Apellido @Model.Nombre?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
                <a href="@Url.Action("Baja", "Propietario", new { id = Model.Id })" class="btn btn-danger">Eliminar</a>
            </div>
        </div>
    </div>
</div> *@
